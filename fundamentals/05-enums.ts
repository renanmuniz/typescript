enum CourseType {
    FREE = "FREE",
    PREMIUM = "PREMIUM",
    PRIVATE = "PRIVATE",
    HIDDEN = "HIDDEN",
}

const course = {
    title: "Typescript Course",
    type: CourseType.PRIVATE
}

console.log(course);